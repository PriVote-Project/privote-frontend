import { IPolicyConfigProps } from '../types';

const HatsPolicyConfig = ({ config, onConfigChange }: IPolicyConfigProps) => {
  return <div>HatsPolicyConfig</div>;
};

export default HatsPolicyConfig;
